# Backend FUT Fullstack AI

Backend Spring Boot para gestionar equipos, jugadores y cartas FUT.

## Arranque

```bash
cd backend
mvn spring-boot:run
```

El backend queda disponible en `http://localhost:8090`.

## Tests

```bash
cd backend
mvn test
```

Los tests usan perfil `test` con H2 en memoria.
